workflows:
  trace-existing-image-component:
    name: Trace Existing Image Component
    instance_type: mac_mini_m2
    max_build_duration: 10
    scripts:
      - name: Find reusable native image component
        script: |
          chmod +x scripts/trace_component.sh
          ./scripts/trace_component.sh
    artifacts:
      - component_trace/**
