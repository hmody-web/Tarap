workflows:
  ads-source-fix-v5:
    name: Build Ads Source Fix V5
    instance_type: mac_mini_m2
    max_build_duration: 30

    scripts:
      - name: Build targeted AdsManager fix
        script: |
          chmod +x build_and_merge.sh inject_dylib.py
          ./build_and_merge.sh

    artifacts:
      - "Tarab_AdsSourceFix_V5.ipa"
