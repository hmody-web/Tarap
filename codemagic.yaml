workflows:
  extract-all-assets:
    name: Extract ALL Assets
    instance_type: mac_mini_m2
    max_build_duration: 30
    scripts:
      - name: Extract every image from Assets.car
        script: |
          chmod +x scripts/extract_all_assets.sh
          ./scripts/extract_all_assets.sh
    artifacts:
      - extracted_assets/**
