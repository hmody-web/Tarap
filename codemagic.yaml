workflows:
  trace-settings-selectors:
    name: Trace Settings Selectors
    instance_type: mac_mini_m2
    max_build_duration: 10
    scripts:
      - name: Map Settings lifecycle selectors
        script: |
          chmod +x scripts/trace_settings_selectors.sh
          ./scripts/trace_settings_selectors.sh
    artifacts:
      - settings_selector_trace/**
