workflows:
  fast-trace-subscription-row:
    name: FAST Trace Mohammed Alsaray Row
    instance_type: mac_mini_m2
    max_build_duration: 15
    scripts:
      - name: Fast trace row to icon
        script: |
          chmod +x scripts/fast_trace.sh
          ./scripts/fast_trace.sh
    artifacts:
      - xref_results/**
