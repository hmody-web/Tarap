workflows:
  layout-cleaner-v3:
    name: Build LayoutCleaner SAFE V3.1
    instance_type: mac_mini_m2
    max_build_duration: 30
    scripts:
      - name: Build SAFE V3
        script: |
          chmod +x build_and_merge.sh inject_dylib.py
          ./build_and_merge.sh
    artifacts:
      - "Tarab_5.11_LayoutCleaner_SAFE_V3_1.ipa"
