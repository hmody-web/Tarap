workflows:
  direct-arm64-trace:
    name: DIRECT ARM64 Trace
    instance_type: mac_mini_m2
    max_build_duration: 10
    scripts:
      - name: Direct binary trace
        script: |
          chmod +x scripts/run_direct_trace.sh
          ./scripts/run_direct_trace.sh
    artifacts:
      - xref_results/**
