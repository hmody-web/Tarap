workflows:
  extract-assets-car:
    name: Extract Assets.car
    instance_type: mac_mini_m2
    max_build_duration: 30
    scripts:
      - name: Extract Assets.car
        script: |
          chmod +x scripts/extract_assets.sh
          ./scripts/extract_assets.sh
    artifacts:
      - extracted_assets/**
