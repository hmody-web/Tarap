workflows:
  trace-setting-row:
    name: Trace SettingRow
    instance_type: mac_mini_m2
    max_build_duration: 10
    scripts:
      - name: Compare subscription and restore rows
        script: |
          chmod +x scripts/trace_settingrow.sh
          ./scripts/trace_settingrow.sh
    artifacts:
      - settingrow_results/**
