workflows:
  trace-image-component-caller:
    name: Trace Image Component Caller
    instance_type: mac_mini_m2
    max_build_duration: 10
    scripts:
      - name: Resolve reusable component caller
        script: |
          chmod +x scripts/trace_caller.sh
          ./scripts/trace_caller.sh
    artifacts:
      - caller_trace/**
