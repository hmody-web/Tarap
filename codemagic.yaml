workflows:
  build-portrait-dylib-early:
    name: Build Portrait Dylib Early
    instance_type: mac_mini_m2
    max_build_duration: 10
    scripts:
      - name: Build early-loading portrait overlay
        script: |
          chmod +x scripts/build.sh
          ./scripts/build.sh
    artifacts:
      - build/PortraitOverlay.framework/**
