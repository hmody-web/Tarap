workflows:
  layout-cleaner-safe-v4:
    name: Build LayoutCleaner SAFE V4
    instance_type: mac_mini_m2
    max_build_duration: 30
    scripts:
      - name: Build SAFE V4
        script: |
          chmod +x build_and_merge.sh inject_dylib.py
          ./build_and_merge.sh
    artifacts:
      - "Tarab_5.11_LayoutCleaner_SAFE_V4.ipa"
