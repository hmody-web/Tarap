workflows:
  build-portrait-dylib:
    name: Build Tarab Portrait Dylib
    instance_type: mac_mini_m2
    max_build_duration: 10
    scripts:
      - name: Build PortraitOverlay framework
        script: |
          chmod +x scripts/build.sh
          ./scripts/build.sh
    artifacts:
      - build/PortraitOverlay.framework/**
