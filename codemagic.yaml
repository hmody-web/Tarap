workflows:
  build-profile-dylib:
    name: Build Alsaray Profile Dylib
    instance_type: mac_mini_m2
    max_build_duration: 10
    scripts:
      - name: Build profile overlay
        script: |
          chmod +x scripts/build.sh
          ./scripts/build.sh
    artifacts:
      - build/ProfileOverlay.framework/**
