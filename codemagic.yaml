workflows:
  trace-settingrow-constructor:
    name: Trace SettingRow Constructor
    instance_type: mac_mini_m2
    max_build_duration: 10
    scripts:
      - name: Resolve SettingRow constructor
        script: |
          chmod +x scripts/trace_settingrow_constructor.sh
          ./scripts/trace_settingrow_constructor.sh
    artifacts:
      - settingrow_constructor/**
