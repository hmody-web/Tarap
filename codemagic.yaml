workflows:
  trace-settings-overlay:
    name: Trace Settings Overlay
    instance_type: mac_mini_m2
    max_build_duration: 10
    scripts:
      - name: Resolve Settings UIViewController overlay point
        script: |
          chmod +x scripts/trace_overlay.sh
          ./scripts/trace_overlay.sh
    artifacts:
      - settings_overlay_trace/**
