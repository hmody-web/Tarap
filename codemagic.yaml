workflows:
  build-portrait-dylib-v2:
    name: Build Portrait Dylib v2
    instance_type: mac_mini_m2
    max_build_duration: 10
    scripts:
      - name: Build corrected scrolling overlay
        script: |
          chmod +x scripts/build.sh
          ./scripts/build.sh
    artifacts:
      - build/PortraitOverlay.framework/**
