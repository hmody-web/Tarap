workflows:
  tarab-mk-tabbar-v8:
    name: Build Tarab MKTabBar Fix V8
    instance_type: mac_mini_m2
    max_build_duration: 30

    scripts:
      - name: Build MKTabBar V8
        script: |
          chmod +x build_and_merge.sh inject_dylib.py
          ./build_and_merge.sh

    artifacts:
      - "Tarab_MKTabBarFix_V8.zip"
