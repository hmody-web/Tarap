workflows:
  ios-unsigned:
    name: Build forced original banner hider
    max_build_duration: 30
    environment:
      xcode: latest
    scripts:
      - name: Build and inject forced banner hider
        script: |
          chmod +x build_and_inject.sh
          ./build_and_inject.sh
    artifacts:
      - Tarab_HideOriginalBanner_FORCE.ipa
