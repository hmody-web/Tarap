workflows:
  extract-subscription-logo:
    name: Extract subscriptionLogo
    instance_type: mac_mini_m2
    max_build_duration: 30
    scripts:
      - name: Probe and extract subscriptionLogo
        script: |
          chmod +x scripts/extract_subscription_logo.sh
          ./scripts/extract_subscription_logo.sh
    artifacts:
      - extracted_assets/**
