workflows:
  build-portrait-dylib-v3:
    name: Build Portrait Dylib v3
    instance_type: mac_mini_m2
    max_build_duration: 10
    scripts:
      - name: Build physical-right scrolling portrait
        script: |
          chmod +x scripts/build.sh
          ./scripts/build.sh
    artifacts:
      - build/PortraitOverlay.framework/**
