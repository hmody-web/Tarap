workflows:
  extract-subscription-logo-v4:
    name: Extract subscriptionLogo V4
    instance_type: mac_mini_m2
    max_build_duration: 30
    scripts:
      - name: Extract CUINamedImage subscriptionLogo
        script: |
          chmod +x scripts/extract_subscription_logo.sh
          ./scripts/extract_subscription_logo.sh
    artifacts:
      - extracted_assets/**
