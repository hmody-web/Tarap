workflows:
  trace-settings-overlay-geometry:
    name: Trace Settings Overlay Geometry
    instance_type: mac_mini_m2
    max_build_duration: 10
    scripts:
      - name: Resolve overlay geometry
        script: |
          chmod +x scripts/trace_geometry.sh
          ./scripts/trace_geometry.sh
    artifacts:
      - settings_overlay_geometry/**
